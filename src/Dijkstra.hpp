#ifndef DIJKSTRA_HPP
#define DIJKSTRA_HPP

#include "Edge.hpp"
#include "Node.hpp"

class Dijkstra {
   private:
    Node* sourceNode;
    Node* destinationNode;
    // run()

   public:
    Dijkstra(/* args */);
    ~Dijkstra();
    // getShortestpath()
};

Dijkstra::Dijkstra(/* args */) {
}

Dijkstra::~Dijkstra() {
}

#endif